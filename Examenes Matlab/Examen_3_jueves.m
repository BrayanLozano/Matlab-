% Examen 3 jueves
%% 1) (1.6)
% Encuentre las posiciones matriciales de los últimos 5 números mayores a
% 0.999 en la matriz 'x', en una sola línea de código.
rng(100) % Semilla del generador de números aleatorios
x = rand(100);

%% 2) (1.8)
% Cargue los datos 'patients'
load patients
% Usando accumarray, calcule el promedio del peso de los pacientes
% fumadores y los no fumadores.

%% 3) (1.6)
% Cree una tabla con 4 columnas: LastName, Gender, Age, Height.
% Organice la tabla según orden ascendente en edad y luego según orden
% descendente en estatura, en una sola línea.